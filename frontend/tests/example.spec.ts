import { test, expect } from 'bun:test';

// Placeholder to avoid Playwright dependency in backend CI runs.
test.skip('login page renders (playwright placeholder)', () => {
  expect(1).toBe(1);
});


